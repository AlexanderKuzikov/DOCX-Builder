# DOCX Builder

**Автор:** Alexander Kuzikov  
**Лицензия:** Apache-2.0  
**Репозиторий:** [github.com/AlexanderKuzikov/DOCX-Builder](https://github.com/AlexanderKuzikov/DOCX-Builder/)

Простая и эффективная утилита на Node.js для пакетной сборки (склеивания) нескольких DOCX файлов в один. Работает через прямую манипуляцию XML-структурой документа, что обеспечивает высокую скорость и отсутствие лишних разрывов страниц.

## 🚀 Возможности

*   **Пакетная обработка:** Сканирует папку `IN` и автоматически собирает документы из всех найденных подпапок.
*   **Умная сортировка:** Собирает части документа в правильном порядке на основе цифрового префикса в имени файла (например, `1_intro.docx`, `2_body.docx`, `10_footer.docx`).
*   **Бесшовная склейка:** Контент объединяется в единый поток текста без принудительных разрывов страниц.
*   **Разделители:** Между частями автоматически вставляется пустая строка для визуального разделения блоков.
*   **Чистота стилей:** Использует стили и настройки страницы (поля, ориентация) из первого файла в комплекте.

## 🛠 Установка

Для работы требуется установленный [Node.js](https://nodejs.org/).

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/AlexanderKuzikov/DOCX-Builder.git
   cd DOCX-Builder
Установите зависимости:

bash
npm install
📋 Как использовать
Создайте папку IN в корне проекта (если её нет).

Внутри папки IN создайте подпапки для каждого документа, который нужно собрать. Имя папки станет именем итогового файла.

Поместите части .docx файлов в соответствующие папки. Имена файлов должны начинаться с цифры для определения порядка сборки.

Пример структуры:

```text
/DOCX-Builder
  ├── builder.js
  └── IN/
       ├── Договор_Поставка/       <-- Будет создан файл "Договор_Поставка.docx"
       │     ├── 1_Преамбула.docx
       │     ├── 2_Условия.docx
       │     └── 3_Реквизиты.docx
       │
       └── Отчет_Январь/           <-- Будет создан файл "Отчет_Январь.docx"
             ├── 10_Введение.docx
             ├── 20_Таблицы.docx
             └── 30_Выводы.docx
text

Запустите скрипт:

bash
node builder.js
Готовые файлы появятся в корне папки IN.

⚠️ Требования к файлам
Для корректной сборки рекомендуется, чтобы:

Все файлы частей были "очищены" (желательно иметь одинаковые базовые стили).

Файлы не содержали сложных конфликтующих зависимостей (хотя скрипт корректно переносит текстовый контент, сложные связи могут требовать дополнительной проверки).

Файл с наименьшим номером (например, 1_*.docx) считается Мастер-файлом: его настройки страницы (поля, ориентация) будут применены ко всему итоговому документу.

📄 Лицензия
Этот проект распространяется под лицензией Apache-2.0. Подробности см. в файле LICENSE.

text
undefined
